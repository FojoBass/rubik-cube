import SectionTemplate from "../templates/SectionTemplate";
import { Section } from "../types";

const Scores = () => {
  return <SectionTemplate id={Section.scores}>Scores</SectionTemplate>;
};

export default Scores;
