import SectionTemplate from "../templates/SectionTemplate";
import { Section } from "../types";

const HallOfFame = () => {
  return <SectionTemplate id={Section.hof}>Hall of Fame</SectionTemplate>;
};

export default HallOfFame;
