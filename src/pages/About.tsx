import SectionTemplate from "../templates/SectionTemplate";
import { Section } from "../types";

const About = () => {
  return <SectionTemplate id={Section.about}>About</SectionTemplate>;
};

export default About;
