import SectionTemplate from "../templates/SectionTemplate";
import { Section } from "../types";

const MainGame = () => {
  return <SectionTemplate id={Section.main}>Main Game</SectionTemplate>;
};

export default MainGame;
