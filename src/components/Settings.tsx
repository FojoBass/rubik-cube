import SectionTemplate from "../templates/SectionTemplate";
import { Section } from "../types";

const Settings = () => {
  return <SectionTemplate id={Section.setts}>Settings</SectionTemplate>;
};

export default Settings;
